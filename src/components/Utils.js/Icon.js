import styled from 'styled-components';

export const Icon = styled.i`
  font-size: 2.5rem;
  color: white;
  margin-bottom: 5px;
  transition: all ease-in-out 0.5s;
  &:hover {
    scale: 1.25;
  }
`;
